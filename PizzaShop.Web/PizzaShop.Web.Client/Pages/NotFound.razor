@page "/notfound"
@inject NavigationManager Navigation

@code {
  protected override void OnInitialized()
  {
    // Fallback for unknown routes: send users to the home page.
    Navigation.NavigateTo("/", forceLoad: true);
  }
}